Plugin to handle dev/acc/prd setting environments for various plugins.

Also first stab to create a 'commons' plugin for an organisation,
bundling common code etc.

Other idea's: one plugin to download/install a common set of plugins